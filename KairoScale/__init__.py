"""KairoScale: ML Training Optimization Pipeline.

Profiles ML training code, generates optimization configs via LLM agent,
validates them in parallel sandboxes, and produces a comparison report.
"""

__version__ = "0.1.0"
